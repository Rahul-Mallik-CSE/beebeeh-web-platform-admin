/** @format */

export interface Product {
  id: string;
  productId: string;
  modelName: string;
  alias: string;
  domesticFreq: string;
  commercialFreq: string;
  parts: number;
  stock: string;
}
